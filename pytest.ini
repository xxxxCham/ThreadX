# pytest.ini
[pytest]
minversion = 7.0
addopts = -q -ra
testpaths = tests
markers =
    integration: tests d'intégration
    slow: tests lents
    slow: tests lents (enregistré pour éviter le warning)
